<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- ===== MAIN CONTENT ===== -->
    <!-- Using FrameLayout instead of CoordinatorLayout -->
    <!-- CoordinatorLayout can interfere with adjustResize in Fragments -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/dark_bg">

        <!-- Toolbar at top -->
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/card_bg"
            android:elevation="4dp"
            android:layout_alignParentTop="true"
            app:title="AIUB eSports Club"
            app:titleTextColor="@color/white"
            app:navigationIconTint="@color/white"/>

        <!-- Fragment Container fills space below toolbar -->
        <FrameLayout
            android:id="@+id/fragmentContainer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/toolbar"/>

        <!-- FAB sits on top of everything -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fabChatbot"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_alignParentEnd="true"
            android:layout_margin="20dp"
            android:src="@android:drawable/ic_dialog_info"
            android:contentDescription="Open Chatbot"
            app:backgroundTint="@color/accent"
            app:tint="@color/white"
            app:fabSize="normal"
            app:elevation="8dp"/>

    </RelativeLayout>

    <!-- ===== NAVIGATION DRAWER ===== -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/card_bg"
        app:headerLayout="@layout/nav_header"
        app:menu="@menu/nav_menu"
        app:itemTextColor="@color/white"
        app:itemIconTint="@color/accent"/>

</androidx.drawerlayout.widget.DrawerLayout>